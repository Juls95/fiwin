/**
 * Phase 9 placeholder. Real implementation arrives in Phase 9.
 */
export async function recallConversation(_args: {
  chatId: number;
  date?: string;
}): Promise<{ date: string; summary: string; source: "daily" | "raw" | "none" }> {
  throw new Error("[tools] recallConversation not implemented yet (Phase 9).");
}
